<!DOCTYPE html>
<html>
  <head>
    <meta charset='utf-8'>
    <title>You created a task!</title>
    <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.1/css/bootstrap.min.css'>
  </head>

  <body>

    <div class='container'>
      <div class='row'>
        <div class='col-md-4'>
          <h3>Your new task!</h3>
          <p>{{ newtask.getTask }}</p>
          <form action='/'>
            <button type='submit' class='btn'>Go back to form</button>
          </form>
        </div>  <!-- .col-md-4 -->
      </div>  <!-- .row -->

      <div class='row'>
          <div class='col-md-3'>
              <hr>
              {% if tasks is not empty %}
                  <h4>Here are all your tasks</h4>
                  <ul>
                      {% for task in tasks %}
                          <li>{{ task.getTask }}</li>
                      {% endfor %}
                  </ul>
              {% endif %}
          </div>
      </div>  <!-- .row -->
    </div>  <!-- .container -->

  </body>
</html>
